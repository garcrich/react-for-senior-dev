{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "playgrounds/*"
    ],
    "nohoist": [
      "**/normalize-scss"
    ]
  },
  "devDependencies": {
    "lerna": "^5.5.0"
  },
  "version": "0.0.0",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "stream": true,
  "scripts": {
    "build": "npx lerna run build"
  }
}
